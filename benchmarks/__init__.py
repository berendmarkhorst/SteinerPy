"""Benchmark harness for SteinerPy (not part of the installable package).

Compares SteinerPy's solvers (baseline vs the opt-in dual-ascent accelerator)
against published SteinLib optima. See ``run_benchmarks.py`` and ``README.md``.
"""
